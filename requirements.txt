# Core requirements for setup, datasets, and shared causal_bench module
numpy
pandas
matplotlib
scikit-learn
torch

# Metalearners (S/T/X-learner, Debiased ML, IPW, DR)
econml>=0.14.0
FLAML[automl]==2.3.5

# Bundled with econml for the metalearners feature set; not currently
# imported by any code here directly (Lalonde loading downloads straight
# from NBER -- see causal_bench/data_loader.py -- no causalml needed for that)
causalml>=0.14.0

# Interactive notebook support
ipywidgets>=8.0.0
nbformat
jupyter

# Model-specific packages are installed inside notebooks as needed:
#   - causalpfn          (pip install causalpfn)
#   - Do-PFN             (git clone; NOT `pip install -r Do-PFN/requirements.txt`
#                        as-is -- pins catboost==1.1.1, no wheel for recent
#                        Python, and DoPFNRegressor doesn't even import it.
#                        Actual runtime deps beyond this file: networkx, tqdm,
#                        einops. Also needs torch<2.10 -- see CLAUDE.md.)
#   - CausalFM-toolkit   (git clone; not pip-installable, added to sys.path)
#                        + einops, tabpfn==2.0.9, tensorboard
#                        (do NOT install the toolkit's own requirements.txt
#                        as-is -- it's a frozen dev env with Linux/CUDA-only
#                        pins. Locally: `uv pip install einops "tabpfn==2.0.9"
#                        tensorboard` -- NOT `uv sync --extra causalfm`, which
#                        reconciles the whole env and uninstalls anything
#                        installed ad hoc outside the declared extras.)
